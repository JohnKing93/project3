import React from "react";
import "./style.css";
import { Container, Row, Col } from 'react-grid-system';

function Wrapper(props) {
  return (
    <main className="wrapper">
      <Container fluid>
      {/* Enter props here is wrapper is used. */}
      </Container>
    </main>
  );
}

export default Wrapper;
